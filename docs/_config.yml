# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: ERPNext Docker Debian
email: pipech@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Readme for https://github.com/pipech/erpnext-docker-debian
baseurl: "/erpnext-docker-debian" # the subpath of your site, e.g. /blog
url: "https://pipech.github.io" # the base hostname & protocol for your site, e.g. http://example.com

# Set navigation links
header_pages:
  - github_code.html
  - setup_docker.md
  - ec2.md

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
